import { FC } from "react";

export const WorkspaceNameDisplay: FC = () => {
  return (
    <div className="text-lg font-semibold">
      {/* This will be populated with actual workspace name later */}
      My Workspace
    </div>
  );
};
